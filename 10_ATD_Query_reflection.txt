АТД Query - рефлексия.

1. Назвал очередь Query вместо Queue. И только на рефлексии заметил. Хотя внутренняя переменная в реализации называется queue.

2. Все методы, которые в эталоне, у меня есть, только с другими названиями.

3. По сравнению с эталоном я добавил метод "Очистить очередь" и запрос "Пустая ли очередь", но пометил их как дополнительные.

4. По сравнению с эталоном у меня ещё есть вариант статуса "в очередь ещё ни одного элемента ни добавлялось". Взял я его из АТД Стека.
Но понял сейчас, что здесь он не нужен (стек реализуется на массиве и желательно выделить состояние "неинициализированности"), а очередь на
связном списке и там инициализировать нечего. 
