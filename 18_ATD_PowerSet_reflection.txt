АТД Множество - рефлексия.

1. В постусловии к конструктору не дописал, что создано на максимальное количество элементов,
то нужно было описать переданные переменные.

2. Не указал, какие типы данных в Powerset (указал PowerSet, а нужно было PowerSet<T>) - это
во всех запросах.

3. Также в описании запросов писать "возвращает..." и не просто с set2, а с "множеством set2",
то есть нужно указывать действие и тип данных у передаваемой переменной нестандартного типа.

4. У меня еще есть запрос Equals сверх запросов в эталоне.
